{% extends 'base.html.twig' %}

{% block title %}Tarea{% endblock %}

{% block body %}
    <h1>Tarea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarea.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ tarea.titulo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tarea.descripcion }}</td>
            </tr>
            <tr>
                <th>Inicio</th>
                <td>{{ tarea.inicio ? tarea.inicio|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ tarea.fin ? tarea.fin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ tarea.tipo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tarea.estado }}</td>
            </tr>
            <tr>
                <th>Progreso</th>
                <td>{{ tarea.progreso }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ tarea.created ? tarea.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ tarea.updated ? tarea.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tarea_index', {'proyecto': proyecto.id}) }}">back to list</a>

    <a href="{{ path('tarea_edit', {'id': tarea.id, 'proyecto': proyecto.id}) }}">edit</a>

    {{ include('tarea/_delete_form.html.twig') }}
{% endblock %}
