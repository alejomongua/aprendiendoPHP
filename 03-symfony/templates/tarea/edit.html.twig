{% extends 'base.html.twig' %}

{% block title %}Editar tarea{% endblock %}

{% block body %}
<div class="container mx-auto my-4">
    <h1 class="text-4xl leading-10 text-green-900 font-light p-4">
        Editar tarea {{ tarea.titulo }}
    </h1>
</div>

<div class="w-full md:w-3/4 lg:w-1/2 xl:w-1/3 mx-auto my-10">
    <div class="py-4 px-10 border bg-green-100 border-green-900 rounded shadow my-4">
        {{ include('tarea/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="my-4">
            <a href="{{ path('tarea_index', {'proyecto': proyecto.id}) }}" class="my-4 p-4 border rounded shadow bg-red-300">
                Cancelar edición
            </a>
        </div>
    </div>

    {{ include('tarea/_delete_form.html.twig') }}
</div>
{% endblock %}
