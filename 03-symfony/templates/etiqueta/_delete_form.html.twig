<form class="w-full" method="post" action="{{ path('etiqueta_delete', {'id': etiqueta.id}) }}" onsubmit="return confirm('¿Está seguro de que quiere eliminar esta etiqueta?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etiqueta.id) }}">
    <button class="p-4 border rounded shadow bg-red-300 mx-10">Eliminar etiqueta</button>
</form>
